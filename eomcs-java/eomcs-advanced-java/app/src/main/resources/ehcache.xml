<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xmlns="http://www.ehcache.org/v3"
        xsi:schemaLocation="http://www.ehcache.org/v3 http://www.ehcache.org/schema/ehcache-core-3.0.xsd">

  <!--
    exam28 - 2차 캐시 Ehcache 설정

    cache alias: Hibernate가 엔티티 캐시 리전 이름으로 사용
      - 엔티티 캐시: "패키지.클래스명" (예: com.eomcs.advanced.jpa.exam28.Customer)
      - 쿼리 캐시:  "org.hibernate.cache.spi.QueryResultsRegion"
      - 타임스탬프: "org.hibernate.cache.spi.TimestampsRegion"

    heap: JVM 힙 메모리 내 캐시 항목 수 제한
    expiry: TTL(Time To Live) 만료 시간
  -->

  <!-- 엔티티 캐시: Customer -->
  <cache alias="com.eomcs.advanced.jpa.exam28.Customer">
    <expiry>
      <ttl unit="minutes">10</ttl>
    </expiry>
    <resources>
      <heap unit="entries">1000</heap>
    </resources>
  </cache>

  <!-- 쿼리 캐시 결과 저장 리전 -->
  <cache alias="org.hibernate.cache.spi.QueryResultsRegion">
    <expiry>
      <ttl unit="minutes">5</ttl>
    </expiry>
    <resources>
      <heap unit="entries">500</heap>
    </resources>
  </cache>

  <!-- 쿼리 캐시 타임스탬프 리전 (무효화 추적용) -->
  <cache alias="org.hibernate.cache.spi.TimestampsRegion">
    <expiry>
      <none/>
    </expiry>
    <resources>
      <heap unit="entries">1000</heap>
    </resources>
  </cache>

</config>
