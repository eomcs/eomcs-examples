## 연결 (Concatenation)

어떤 산술 연산이 비숫자형 데이터에도 적용될 수 있다는 것을 기억하시나요?  
`+` 기호를 사용해 두 문자열을 결합하는 것을 연결(concatenation)이라고 합니다.  

`hello`와 `world` 변수를 사용하여 문자열 `"Hello World"`를 만들어 보세요.  

<div class='hint'>연속된 연결과 한 칸 공백 문자열 <code>" "</code>을 사용하세요.</div>